<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="com.zqh.mapper.LikeMapper" >
    <select id="selectLike" resultType="com.zqh.pojo.LikeRecord">
        select * from like_record
        where doctor_id = #{doctorId} and user_ip = #{userIp}
    </select>

    <insert id="insertLike" parameterType="com.zqh.pojo.LikeRecord">
        insert into like_record (doctor_id , user_ip, created_time)
        values (#{doctorId}, #{userIp}, NOW())
    </insert>

    <delete id="deleteLike">
        delete from like_record where id = #{id}
    </delete>
</mapper>